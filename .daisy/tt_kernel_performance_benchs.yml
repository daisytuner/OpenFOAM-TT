on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

parameters:
  container: daisytuner/x64-tenstorrent:0.63.0-latest
  timeout: 120
  partitions:
    - chamomile

steps:
  build: |
    apt-get update
    apt-get install -y build-essential libopenmpi-dev zlib1g-dev gnuplot gnuplot-x11 libxt-dev cmake flex
    apt-get install -y libxml2-dev libhdf5-dev libavfilter-dev libtheora-dev libgl2ps-dev libx11-dev libqt5x11extras5-dev libglew-dev libutfcpp-dev 

    # Set up OpenFOAM environment
    cd 3rdParty/OpenFOAM-dev/
    export WM_PROJECT_DIR=$(pwd)
    . ./etc/custom_bashrc

    # Generate include files
    ./wmake/wmakeLnIncludeAll

    # Build tests with CMake and Ninja

    # Set up DAISY environment for rtl
    export CPATH=$DOCC_ROOT/include:$CPATH
    export LIBRARY_PATH=$DOCC_ROOT/lib:$LIBRARY_PATH

    # Step 1: Compiler instrumented binaries
    cd ../../
    mkdir build && cd build
    cmake -G Ninja -DENABLE_TT=ON -DENABLE_DAISY_RTL=ON -DTT_IMPL=TT_IMPL_ELLPACK -DCMAKE_BUILD_TYPE=RelWithDebInfo ..
    ninja

    # Rename libOpenFOAM.so to libOpenFOAM.instrumented.so to avoid conflicts
    mv libOpenFOAM.so libOpenFOAM.instrumented.so
    
    cp tt-bench-harness/tt_bench_dense_Amul.out tt-bench-harness/tt_bench_dense_Amul.instrumented.out
    cp tt-bench-harness/tt_bench_dense_Amul_test.out tt-bench-harness/tt_bench_dense_Amul_test.instrumented.out

    cp tt-bench-harness/tt_bench_ellpack_Amul.out tt-bench-harness/tt_bench_ellpack_Amul.instrumented.out
    cp tt-bench-harness/tt_bench_ellpack_Amul_no_hw.out tt-bench-harness/tt_bench_ellpack_Amul_no_hw.instrumented.out

    mkdir -p /tmp/DOCC/000000/123456789
    cp ../tt-bench-harness/sdfg_0.json /tmp/DOCC/000000/123456789/sdfg_0.json
    cp ../tt-bench-harness/sdfg_0.json /tmp/DOCC/000000/123456789/sdfg_1.json
    cp ../tt-bench-harness/sdfg_0.json /tmp/DOCC/000000/123456789/sdfg_2.json
    cp ../tt-bench-harness/sdfg_0.json /tmp/DOCC/000000/123456789/sdfg_3.json

    # Step 2: Compiler non-instrumented binaries
    cmake -G Ninja -DENABLE_TT=ON -DENABLE_DAISY_RTL=OFF -DCMAKE_BUILD_TYPE=RelWithDebInfo ..
    ninja

    # CLI runners will automatically measure application performance with non-instrumented binaries
    # and then run with instrumented binaries for coverage data collection

  run:
    denseMatrix_Amul:
      command: ./build/tt-bench-harness/tt_bench_dense_Amul.out 128 128
      env:
        WM_PROJECT_DIR: /workspace/3rdParty/OpenFOAM-dev
        LD_LIBRARY_PATH: /workspace/build:$LD_LIBRARY_PATH
        TT_FOAM_KERNEL_DIR: /workspace/daisy-tt-rt/src/tenstorrent-kernels
    denseMatrix_Amul_test:
      command: ./build/tt-bench-harness/tt_bench_dense_Amul_test.out
      env:
        WM_PROJECT_DIR: /workspace/3rdParty/OpenFOAM-dev
        LD_LIBRARY_PATH: /workspace/build:$LD_LIBRARY_PATH
        TT_FOAM_KERNEL_DIR: /workspace/daisy-tt-rt/src/tenstorrent-kernels
    ellpackMatrix_Amul_fpu:
      command: ./build/tt-bench-harness/tt_bench_ellpack_Amul.out
      env:
        WM_PROJECT_DIR: /workspace/3rdParty/OpenFOAM-dev
        LD_LIBRARY_PATH: /workspace/build:$LD_LIBRARY_PATH
        TT_FOAM_KERNEL_DIR: /workspace/daisy-tt-rt/src/tenstorrent-kernels

    ellpackMatrix_Amul_no_hw:
      command: ./build/tt-bench-harness/tt_bench_ellpack_Amul_no_hw.out
      env:
        WM_PROJECT_DIR: /workspace/3rdParty/OpenFOAM-dev
        LD_LIBRARY_PATH: /workspace/build:$LD_LIBRARY_PATH
        TT_FOAM_KERNEL_DIR: /workspace/daisy-tt-rt/src/tenstorrent-kernels